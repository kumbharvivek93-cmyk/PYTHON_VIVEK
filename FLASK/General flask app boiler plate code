from flask import Flask,render_template,redirect,session,flash,request,url_for
from flask_sqlalchemy import SQLAlchemy  # module importing section
from datetime import timedelta

app=Flask(__name__)
app.config['SQLALCHEMY_DATABASE_URI']='sqlite:///db.name.sqlite3'
app.config['SQLALCHEMY_TRACK_MODIFICATIONS']=False   # instace making section 
app.permanent_session_lifetime=timedelta(days=2)
app.secret_key='vivekkali'
db=SQLAlchemy(app)

class dbname(db.model):
    id=db.Column(db.Integer,primary_key=True)  # database section 

# if app contains any score or dicision or any other etc. functions then code them here

with app.app_context():
    db.create_all()                     
    print("database is created")

@app.route("/")      # route section
def home():
    return render_template("index.html")

# routes and fuctions code here

if __name__=='__main__':  # running conditions
    app.run(debug=True)
