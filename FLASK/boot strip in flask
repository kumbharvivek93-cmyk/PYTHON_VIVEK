Adding boot strip in flask meaning to use the css framrworks for morden aand profectional looks and all greate things

it dose minimizes writing the css by ourself and gives bueafiful buttons ul , touch responses and so many things

With Bootstrap:

Beautiful buttons

Responsive grid system

Navbar

Cards

Forms look professional

Works on mobile automatically

also plain html looks so basic and non techi so bootstrip is the thing that we can look for !!


by bypassing the frontend frameworks such as react angular etc.
