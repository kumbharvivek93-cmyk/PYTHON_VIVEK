to do password hashing with werkzeug we do follow the follwing steps 

step1 :
import the werkzeug.security module import the generate_password_hash and check_password_hash

>>> from werkzeug.secruty import generate_password_hash,check_password_hash

step2 :
at the point we writing the sqlalchemys database write in the same block 

password_hash=db.Column(db.String(200))  # this password column general

@property 
def password(self):
  raise AttributeError ("something went wrong ")

@password.setter
def password(self,password):
  self.password_hash=generate_password_hash(password)

def check_password(self,password):
return check_password_hash(self.password_hash,password)

# all done buddy    ~ vivek sir ( spidey aidev ) love you all 
