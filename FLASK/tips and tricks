handel the data of app like a pro 

i,e for adding the new user in database we do the 

db.session.add(new_user)
db.session.commit()

put these statements in try and except block 

try :
  db.session.add(new_user)
  db.session.commit()

except :
  db.session.rollback()

====================================================================================================
never store the plain password hashed it well 

from werkzeug.security import generate_password_hash, check_password_hash

hashed = generate_password_hash(password)
check_password_hash(hashed, password)
